#include "main.h"

/**
 * Checks if a character is a lowercase letter.
 * 
 * @param c The character to be checked.
 * @return Returns 1 if c is a lowercase letter, otherwise 0.
 */
int is_lowercase(int c)
{
    return (c >= 'a' && c <= 'z');
}

