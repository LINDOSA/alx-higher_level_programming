#include "main.h"

/**
 * Checks if a given character is a digit (0 through 9).
 * 
 * @param c The integer value to be checked.
 * @return Returns 1 if c is a digit, otherwise 0.
 */
int is_digit(int c)
{
    return (c >= '0' && c <= '9');
}

