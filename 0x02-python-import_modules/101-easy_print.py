#!/usr/bin/python3

# Import the subprocess module to run external commands
import subprocess

# Define a command as a list of strings
command = ["echo", "#pythoniscool"]

# Use the subprocess module to run the defined command
subprocess.call(command)

